	<div>
		<p>Categories</p>
		<% Category.all.each do |category| %>
			<div>
				<p>
					<% if category.category_image%>
					<%= link_to image_tag(category.category_image.image_url, :size => "200x100"), items_path(:cpath=>category) %>	
					<% else%>
					<%= link_to category.name, items_path(:cpath=>category)%>
					<% end%>
				</p>
			</div>
		<%end%>
		<p><%= link_to 'Uncategorized', items_path(:uncategorized=>true)%></p>
	</div>
	<hr />
	
	<!--	<div>
		<%= form_tag items_path, :method => 'get' do %>
			Price:<%= number_field_tag :low, params[:low], :class=>"sidebar_number_field" %>-<%= number_field_tag :high, params[:high], :class=>"sidebar_number_field" %></p>
			<%= submit_tag 'Filter', :name=>nil, :class=>"btn"%>
		<% end %>
		</p>
	</div> -->

	<div class="btn-group">
	  <a class="btn dropdown-toggle" data-toggle="dropdown">
	    Price Range
	    <span class="caret"></span>
	  </a>
	  <ul class="dropdown-menu">
	    <li><%= link_to "$0.00 - $100.00", items_path(:low=>0, :high=>100)%></li>
	    <li><%= link_to "$100.00 - $250.00", items_path(:low=>100, :high=>250)%></li>
	    <li><%= link_to "$250.00 - $500.00", items_path(:low=>250, :high=>500)%></li>
	    <li><%= link_to "$500.00 - $1000.00", items_path(:low=>500, :high=>1000)%></li>
	    <li><%= link_to "$1000.00+", items_path(:low=>1000)%></li>
	  </ul>
	</div>

	
	<!-- <ul class="nav nav-pills">
	  <li class="dropdown" id="menu1">
	    <a class="dropdown-toggle" data-toggle="dropdown" href="#menu1">
	      Price Range
	      <b class="caret"></b>
	    </a>
			<% @lowest = Item.minimum('price') %>
			<% @highest = Item.maximum('price') %>
			<% @increse = (@highest-@lowest)/4%>
	    <ul class="dropdown-menu">
				<% @current = @lowest %>
				<% 4.times do |i| %>
						<% @lowbound = @lowest+i*@increse%>
				    <% @upbound = @lowest+(i+1)*@increse%>
						<li><a href="#">$<%= @lowbound.to_s %>----$<%= @upbound.to_s %></a></li>
				<% end %>
	    </ul>
	  </li>
	</ul> -->