<div class='remove-magrin'>
	<h4>Categories</h4>
	<% Category.all.each do |category| %>
	<div remove-magrin>
		<p>
			<% if category.image%>
			<%= link_to image_tag(category.image, :size => "200x60"), items_path(:cpath=>category) %>	
			<% else%>
			<%= link_to category.name, items_path(:cpath=>category)%>
			<% end%>
		</p>
	</div>
	<%end%>
	<p><%= link_to 'Uncategorized', items_path(:cpath=>-1)%></p>
</div>
<hr/>

<br>

<div id ='wishlist'>
	<% if cookies['wishlist'] %>
	<% cookies['wishlist'].split(",").each do |wl| %>
	<% item_wl = Item.find(wl.to_i) %>
	<% if item_wl %>
	<%= link_to item_wl do %>
	<p>
		<span> <%= item_wl.name %> : </span>
		<span> $ <%= item_wl.price %> </span>
	</p>
	<% end %>
	<% end %>
	<% end %>
	<div>
		<a id="clean_wishlist">Clean Wishlist</a>
	</div>
	<script type="text/javascript">
	$("#clean_wishlist").click(function(){
		$.cookie('wishlist', null);
		$('#wishlist').html("");
		$('.interest').removeClass('btn-danger').addClass('btn-success');
	});
	</script>
	<% end %>
</div>
